{
  "name": "dip-peerserver",
  "version": "0.0.0",
  "private": true,
  "description": "Pinned local PeerJS signalling server. Started before the runtime page; see docs/EXPERIMENTS.md.",
  "scripts": {
    "start": "peerjs --port 9000 --host 0.0.0.0"
  },
  "dependencies": {
    "peer": "1.0.2"
  }
}
